const hostname = window.location.hostname;

export const BASE_URL = `http://${hostname}:5002`;
export const SERVER_URL = `http://${hostname}:5002`; // Keeping for backward compatibility
export const AI_URL = `http://${hostname}:8000`;
